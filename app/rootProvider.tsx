"use client";
import { ReactNode } from "react";

export function RootProvider({ children }: { children: ReactNode }) {
  // Basit placeholder - OnchainKit dependency'si kaldırıldı
  return <>{children}</>;
}
